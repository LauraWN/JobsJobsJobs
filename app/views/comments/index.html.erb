<h2>Comments</h2>

<ul>
  <% @comments.each do |comment| %>
    <li>
      <%= link_to "#{comment.title} - #{comment.post.title}",post_comment_path(post.comment,comment) %>
    </li>
  <% end %>
</ul>
